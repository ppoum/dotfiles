# Dotfiles

## Installation
This repository requires GNU stow to be installed. Each subdirectory can be installed by running `stow <dir>`.

## zsh
The `zsh` directory requires `oh-my-zsh` to be installed.
